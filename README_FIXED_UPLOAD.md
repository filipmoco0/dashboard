# Fixed Google Drive upload

This version removes the duplicate upload button and adds the Google Drive connect panel inside Settings.

Replace these files in GitHub:

- app.js
- index.html
- style.css
- config.js

Then Render will redeploy. Open the site and hard refresh.

Settings now shows:

- Google Drive status
- connect / reconnect
- disconnect

There should be only one upload button in the top-right next to + add.
